{# On récupére notre base #}
{% extends 'base.html.twig' %}

{# On redéfinit le titre d'onglet #}
{# {% block title %}Site de jeux vidéos{% endblock %} #}
	{% block title %}
{{title|default('Nos films')}}
{% endblock %}

{# On définit le contenu de la page #}
	{% block body %}
		<div class="container mt-5"> <header class="mb-4">
			<h1 class="text-center mb-5 display-4">{{title|default('Nos films')}}</h1>
		</header>

		<div class="game-grid">
			{% for film in film %}
				{% include 'components/_film_card.html.twig' with {'film': film} %}
			{% endfor %}
		</div>
	</div>
{% endblock %}
