{% extends 'base.html.twig' %}

{% block title %}Edition des genres
{% endblock %}

{% block body %}
	<div class="game-form-container">

		<h1>Modifier le genre
			{{ genre.label }}</h1>


		{% include "genre/_form.html.twig" with {'button_label': 'Modifier'} %}

		<div class="action-buttons">

			<a href="{{ path('app_genre_index') }}" class="btn btn-back">Retour à la liste</a>


			{{ include('genre/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}