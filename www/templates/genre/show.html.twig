{% extends 'base.html.twig' %}

{% block title %}Genre
{% endblock %}

{% block body %}
	<div class="game-list-container">

		<h1>{{genre.label}}</h1>

		<table class="game-table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Label</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>{{ genre.id }}</td>
					<td>{{ genre.label }}</td>
				</tr>
			</tbody>
		</table>

		<div class="action-buttons">
			<div>
				{# suppression #}
				{% include "genre/_delete_form.html.twig" with {'button_class': 'btn btn-delete'} %}
			</div>
			<div>
				<a href="{{ path('app_genre_edit' , {'id': genre.id}) }}" class="btn btn-edit">Editer</a>


			</div>
			<div>
				<a href="{{path('app_genre_index')}}" class="btn btn-back">

					<i class="fas fa-arrow-left"></i>Retour à la liste</a>

			</div>

		</div>

	{% endblock %}